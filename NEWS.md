# tidytargets NEWS

## tidytargets 0.1.0

* Extracted the reusable tidy `{targets}` grammar from HPCell
  (`initialise_hpc()`, `hpc_iterate()`, `hpc_single()`, `hpc_merge()`,
  `hpc_report()`, `evaluate_hpc()`).
* Dropped single-cell RNA analysis modules, data, and reports.
