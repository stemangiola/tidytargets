% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_explore.R
\name{tt_explore}
\alias{tt_explore}
\alias{tt_explore.default}
\alias{tt_explore.tidytargets}
\title{Return One Instance of a Pipeline Target}
\usage{
tt_explore(tt_input, target_output, index = 1L)

\method{tt_explore}{default}(tt_input, target_output, index = 1L)

\method{tt_explore}{tidytargets}(tt_input, target_output, index = 1L)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object from \code{tt_initialise()}.}

\item{target_output}{Name of the target to inspect, unquoted (\code{data}) or
as a string (\code{"data"}), like \code{targets::tar_read()}.}

\item{index}{1-based index of the instance to return when the target has
more than one. Default: \code{1}.}
}
\value{
The retrieved instance.
}
\description{
Reads a stored target from a \code{tidytargets} pipeline and returns a single
instance so you can inspect it or pipe it onward. A short heading is
issued with \code{message()} (target name and, for collections, which instance).
The object itself is returned, not printed: a top-level call still shows
it because R auto-prints the result; a piped call does not.

For a mapped (patterned) target this is one branch, loaded without pulling
every branch into memory. For a mapped list stored as a single stem
(e.g. \code{input_list}) this is one list element. For a non-mapped target the
whole object is returned.

If the target has not been built yet, the pipeline is evaluated first
(same incremental \code{tar_make()} as \code{tt_evaluate()}).
}
\examples{
\dontrun{
pipeline |> tt_explore(data)
pipeline |> tt_explore(data) |> summary()
pipeline |> tt_explore("data", index = 2)
}
}
