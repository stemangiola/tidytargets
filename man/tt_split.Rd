% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_split.R
\name{tt_split}
\alias{tt_split}
\alias{tt_split.default}
\alias{tt_split.tidytargets}
\title{Split a Stem Target into Mapped Units}
\usage{
tt_split(
  tt_input,
  command = NULL,
  target_output = NULL,
  n_units = NULL,
  user_function_source_path = NULL,
  ...
)

\method{tt_split}{default}(
  tt_input,
  command = NULL,
  target_output = NULL,
  n_units = NULL,
  user_function_source_path = NULL,
  ...
)

\method{tt_split}{tidytargets}(
  tt_input,
  command = NULL,
  target_output = NULL,
  n_units = NULL,
  user_function_source_path = NULL,
  ...
)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object.}

\item{command}{An unevaluated expression that returns a list. Write
\code{name <- expr} to name the target from the assignment
(\code{tt_split(settings <- grid |> group_split(row_number()))}).
\code{{targets}} tracks dependencies from global symbols in the command
(the right-hand side if you used \verb{<-}). \code{=} inside the call is
argument matching, not assignment; use \verb{<-}.}

\item{target_output}{Character name of the output target. Optional if
\code{command} is \code{name <- expr}.}

\item{n_units}{Optional positive integer override for the number of
list elements. \code{NULL} (the default) infers it from the command when
possible.}

\item{user_function_source_path}{Optional character path to an R script
to source in the worker before evaluating \code{command}. \code{NULL} sources
nothing.}

\item{...}{Additional factory arguments such as \code{format}, \code{deployment},
or \code{packages}.}
}
\description{
Inverse of \code{\link[=tt_merge]{tt_merge()}}: appends a non-patterned target whose command
returns a list, and marks it as mapped units so later \code{\link[=tt_iterate]{tt_iterate()}}
steps that mention \code{target_output} branch over the elements.

Use this when the list is produced \strong{in the pipeline} (for example
splitting a \code{\link[=tt_data]{tt_data()}} grid). To snapshot a list from the current
session, use \code{\link[=tt_data_list]{tt_data_list()}} instead.

The number of units is taken from \code{length()} of the command result when
that can be evaluated in the current session (typical after \code{\link[=tt_data]{tt_data()}}).
Pass \code{n_units} only to override, or when the length is not known yet.
}
