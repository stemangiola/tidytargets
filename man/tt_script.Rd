% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{tt_script}
\alias{tt_script}
\title{Write the Targets Script Without Running the Pipeline}
\usage{
tt_script(pipe)
}
\arguments{
\item{pipe}{A \code{tidytargets} object from \code{\link[=tt_initialise]{tt_initialise()}}.}
}
\value{
The script path, invisibly.
}
\description{
\verb{\{store\}.R} is generated from the pipeline object when the pipeline runs.
Call this to write it early, so the \code{{targets}} functions that read a
script can inspect the graph before anything executes, for example
\code{targets::tar_manifest()}, \code{targets::tar_visnetwork()},
\code{targets::tar_outdated()}.

The pipeline's script and store are registered with
\code{targets::tar_config_set()}, so those functions then need no arguments.
That writes \verb{_targets.yaml} in the working directory, which is how
\code{{targets}} is told to use a script other than \verb{_targets.R}.

Nothing is executed; use \code{\link[=tt_evaluate]{tt_evaluate()}} to run the pipeline.
}
\examples{
\dontrun{
pipe <- tt_initialise(store = "store") |> tt_single(n <- 1)
tt_script(pipe)
targets::tar_manifest()
targets::tar_visnetwork()
}

}
