% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_single.R
\name{tt_single}
\alias{tt_single}
\alias{tt_single.default}
\alias{tt_single.tidytargets}
\title{Add a Summarising (Non-Iterated) Step to the tidytargets Pipeline}
\usage{
tt_single(
  tt_input,
  command = NULL,
  target_output = NULL,
  user_function_source_path = NULL,
  ...
)

\method{tt_single}{default}(
  tt_input,
  command = NULL,
  target_output = NULL,
  user_function_source_path = NULL,
  ...
)

\method{tt_single}{tidytargets}(
  tt_input,
  command = NULL,
  target_output = NULL,
  user_function_source_path = NULL,
  ...
)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object.}

\item{command}{An unevaluated expression. Write \code{name <- expr} to name the
target from the assignment (\code{tt_single(n <- length(x))}). \code{{targets}}
tracks dependencies from global symbols in the command (the right-hand
side if you used \verb{<-}). \code{=} inside the call is argument matching, not
assignment; use \verb{<-}.}

\item{target_output}{Character name of the output target. Optional if
\code{command} is \code{name <- expr}.}

\item{user_function_source_path}{Optional character path to an R script to
source in the worker before evaluating \code{command}. \code{NULL} sources nothing.}

\item{...}{Additional factory arguments such as \code{format}, \code{deployment},
or \code{packages}.}
}
\description{
Appends one non-iterated targets step: a whole object in, a single object
out. Use \code{\link[=tt_iterate]{tt_iterate()}} when the step should be mapped or crossed over
units. Use \code{\link[=tt_data_list]{tt_data_list()}} to bring a session list in as units, or
\code{\link[=tt_split]{tt_split()}} to turn a pipeline stem into units.
}
