% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_metadata.R
\name{tt_metadata}
\alias{tt_metadata}
\alias{tt_metadata.default}
\alias{tt_metadata.tidytargets}
\title{Get or Set Free-Form Metadata on a tidytargets Object}
\usage{
tt_metadata(tt_input, ...)

\method{tt_metadata}{default}(tt_input, ...)

\method{tt_metadata}{tidytargets}(tt_input, ...)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object.}

\item{...}{Named values to store. Omit to read the metadata instead.}
}
\value{
The metadata list when reading, or the updated \code{tidytargets} object
when writing.
}
\description{
Reads and writes the metadata store of a \code{tidytargets} object, a free-form
named list for information that is not part of the pipeline itself, such as
API endpoints, credentials handles, dataset identifiers or provenance notes.

Called with no additional arguments the metadata list is returned. Called
with named arguments the entries are merged into the existing metadata and
the \code{tidytargets} object is returned, so the call can sit anywhere in a
pipeline. Passing \code{NULL} as a value removes that entry.

Metadata lives in the pipeline object only; it is not written to the targets
script and is not available to workers. To pass values to workers, include
them in the \code{command} expression of \code{tt_iterate()} or \code{tt_single()}.
}
\details{
The store is held under a dot-prefixed element of the object. \code{targets}
rejects target names beginning with a dot, so the store can never be shadowed
by a target the user adds, and metadata places no restriction on the names
passed to \code{target_output}.
}
\examples{
\dontrun{
pipeline <- tt_initialise() |>
  tt_metadata(api_url = "https://api.example.org", api_version = 2)

pipeline |> tt_metadata()
tt_metadata(pipeline)$api_url
}
}
