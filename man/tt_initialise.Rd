% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_initialise.R
\name{tt_initialise}
\alias{tt_initialise}
\title{Initialise a tidytargets Pipeline}
\usage{
tt_initialise(
  tt_input = NULL,
  store = NULL,
  computing_resources = NULL,
  debug_step = NULL,
  verbosity = targets::tar_config_get("reporter_make"),
  error = "continue",
  update = "thorough",
  garbage_collection = 0,
  workspace_on_error = FALSE,
  packages = attached_packages(),
  target_output = "input_list"
)
}
\arguments{
\item{tt_input}{Named vector of inputs, typically file paths, or a named
list of in-memory objects, one element per unit of iteration (e.g. sample).
If names are not set, integer indices are used. \code{NULL} (the default)
writes only the script header; add objects later with \code{\link[=tt_data]{tt_data()}} or
pass a list here to map over.}

\item{store}{Directory path where pipeline files and targets store are written.
\code{NULL} (the default) writes to \verb{./tidytargets-<HASH>} in the working
directory and prints that path.}

\item{computing_resources}{A controller object accepted by
\code{targets::tar_option_set(controller = )}, such as a \code{crew} controller or
controller group. \code{NULL} (the default) runs the pipeline sequentially.
tidytargets does not depend on any compute backend; pass whatever your
deployment uses.}

\item{debug_step}{Character name of a single target to debug; passed to
\code{targets::tar_option_set(debug = ...)}. \code{NULL} disables debugging.}

\item{verbosity}{Reporter string passed to \code{targets::tar_make()}. Defaults to
the current targets configuration value.}

\item{error}{Error-handling strategy passed to \code{targets::tar_option_set()}.
Default: \code{"continue"} (keep running other targets after a failure).
Use \code{"stop"} to halt the pipeline on the first error.}

\item{update}{Cue mode string for \code{targets::tar_cue()}, controlling when
targets are re-run. Default: \code{"thorough"}.}

\item{garbage_collection}{Numeric interval (in targets) at which R garbage
collection is triggered during the pipeline run. Default: \code{0} (disabled).}

\item{workspace_on_error}{Logical; if \code{TRUE}, saves a workspace snapshot when
a target errors. Default: \code{FALSE}.}

\item{packages}{Character vector of R packages loaded on workers, written to
\code{tar_option_set(packages = )}. The default is packages currently attached in
the session (\code{\link[=.packages]{.packages()}}), as names only — not objects in the global
environment. Pass a character vector to override. \code{"qs2"} is always
included. The names written to workers are messaged so you can see what
HPC nodes will need to have installed.}

\item{target_output}{Character name of the mapped input target. Default:
\code{"input_list"}. Ignored when \code{tt_input} is \code{NULL}. A companion
file-tracking target is registered as \verb{\{target_output\}_file}.}
}
\value{
A \code{tidytargets} S3 object with \verb{$initialisation} (constructor
arguments), \verb{$metadata} (see \code{\link[=tt_metadata]{tt_metadata()}}), and \verb{$targets} (named
step records), ready to be extended with pipeline step functions. The
graph is not run until you print it or call \code{\link[=tt_evaluate]{tt_evaluate()}}.
}
\description{
Sets up and writes a \code{targets} pipeline script. Saves configuration (and
optional mapped inputs) to disk, then returns a \code{tidytargets} object that
downstream grammar functions (e.g. \code{tt_data()}, \code{tt_iterate()},
\code{tt_single()}, \code{tt_evaluate()}) can extend before the pipeline is executed
with \code{tt_evaluate()}. The graph is not run until you print the object or
call \code{\link[=tt_evaluate]{tt_evaluate()}}. Assigning it does not; an interactive session then
says the pipeline is ready to be evaluated, rather than appearing to do
nothing.
}
