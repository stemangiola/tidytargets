% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_import.R
\name{tt_import}
\alias{tt_import}
\alias{tt_import.default}
\alias{tt_import.tidytargets}
\title{Import a Session Object as a Pipeline Target}
\usage{
tt_import(tt_input, x, target_output = NULL)

\method{tt_import}{default}(tt_input, x, target_output = NULL)

\method{tt_import}{tidytargets}(tt_input, x, target_output = NULL)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object from \code{tt_initialise()}.}

\item{x}{Object in the current session to snapshot into the store.}

\item{target_output}{Character name of the target. \code{NULL} (the default) uses
the symbol supplied as \code{x}, or the left-hand side of \code{x <- value}.}
}
\value{
The updated \code{tidytargets} object.
}
\description{
Snapshots an object from the current R session onto the pipeline store and
registers it as a single (non-mapped) target. Unlike \code{tt_initialise()}, the
object is not split into iteration units: a list or a Bioconductor object
is stored as one value.

The value is written to disk immediately (\code{qs_save()}). \code{{targets}} then
tracks that file and reads it back when the target runs, so later
\code{tt_single()} / \code{tt_iterate()} commands can use \code{target_output} as a
dependency. A local variable mentioned only in \code{command} is not imported;
use this function for that.

If \code{target_output} is omitted, the name of \code{x} is used
(\code{tt_import(pipeline, airway)} registers \code{"airway"}), or the left-hand
side of an assignment (\code{tt_import(pipeline, airway <- se)}).

For a list of units to map over (for example each row of a parameter grid),
use \code{\link[=tt_import_list]{tt_import_list()}} instead.
}
\examples{
\dontrun{
pipeline <- tt_initialise(store = "store") |>
  tt_import(airway, target_output = "airway")
}
}
