% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_evaluate.R
\name{tt_evaluate}
\alias{tt_evaluate}
\alias{tt_evaluate.default}
\alias{tt_evaluate.tidytargets}
\title{Execute the tidytargets Pipeline}
\usage{
tt_evaluate(tt_input)

\method{tt_evaluate}{default}(tt_input)

\method{tt_evaluate}{tidytargets}(tt_input)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object constructed by \code{tt_initialise()} and
extended with one or more pipeline step functions.}
}
\value{
A \code{tibble} with targets metadata.
}
\description{
Closes the pipeline target list and calls \code{targets::tar_make()} to execute
all queued steps. Returns the \code{tar_meta()} table.

If a target fails with an S4 method-dispatch error on a \code{list} (typically
because a list was brought in with \code{\link[=tt_data]{tt_data()}} instead of
\code{\link[=tt_data_list]{tt_data_list()}}), the error is rethrown with a hint to use
\code{tt_data_list()}. If \code{{targets}} errors because \code{map()} inputs have
unequal lengths, the error is rethrown with a hint to use
\code{pattern = "cross"} (or \code{tt_data()} for a length-1 constant).

The generic records that this store has been run before dispatching, so a
subclass \code{tt_evaluate} method cannot leave the interactive "pipeline is
ready" notice standing after a result that has already been shown.
}
