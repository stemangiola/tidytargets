% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_read.R
\name{tt_read}
\alias{tt_read}
\alias{tt_read.default}
\alias{tt_read.tidytargets}
\title{Read a Stored Pipeline Target}
\usage{
tt_read(tt_input, target_output)

\method{tt_read}{default}(tt_input, target_output)

\method{tt_read}{tidytargets}(tt_input, target_output)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object from \code{tt_initialise()}.}

\item{target_output}{Name of the target to read, unquoted (\code{data}) or
as a string (\code{"data"}), like \code{targets::tar_read()}.}
}
\value{
The stored target value.
}
\description{
Reads a stored target from a \code{tidytargets} pipeline and returns the full
value. For a mapped (patterned) target this is every branch (typically a
list). For a non-mapped target this is the stored object.

If the target has not been built yet, the pipeline is evaluated first
(same incremental \code{tar_make()} as \code{tt_evaluate()}).

Compare with \code{\link[=tt_explore]{tt_explore()}}, which returns one instance at a time without
loading every branch.
}
\examples{
\dontrun{
pipeline |> tt_read(data)
pipeline |> tt_read("data")
}
}
