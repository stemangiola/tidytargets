% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tt_data.R
\name{tt_data_list}
\alias{tt_data_list}
\alias{tt_data_list.default}
\alias{tt_data_list.tidytargets}
\title{Add a List of Units as a Mapped Pipeline Target}
\usage{
tt_data_list(tt_input, x, target_output = NULL)

\method{tt_data_list}{default}(tt_input, x, target_output = NULL)

\method{tt_data_list}{tidytargets}(tt_input, x, target_output = NULL)
}
\arguments{
\item{tt_input}{A \code{tidytargets} object from \code{tt_initialise()}.}

\item{x}{A list (or list-like object, such as the result of
\code{dplyr::group_split()}). Each element becomes one mapped unit.}

\item{target_output}{Character name of the mapped target. \code{NULL} (the
default) uses the symbol supplied as \code{x}, or the left-hand side of
\code{x <- value}.}
}
\value{
The updated \code{tidytargets} object.
}
\description{
Snapshots a list from the current R session onto the store and registers it
as a \strong{mapped} target, one iteration unit per element. This is the mapped
analogue of passing a named list to \code{\link[=tt_initialise]{tt_initialise()}}: later
\code{\link[=tt_iterate]{tt_iterate()}} steps that mention \code{target_output} are mapped over the
elements.

When a later \code{\link[=tt_iterate]{tt_iterate()}} command mentions more than one mapped target,
\code{{targets}} \code{map()} is used by default. Length-1 lists are omitted from
\code{map()} and do not decide whether sizes match. Different lengths error;
pass \code{pattern = "cross"} for a product of branches.

Typical use is a parameter grid split into rows, e.g.
\code{tt_data_list(settings <- grid |> split(seq_len(nrow(grid))))}
or \code{dplyr::group_split()}. Unnamed lists are named with integer indices,
the same way \code{\link[=tt_initialise]{tt_initialise()}} names unnamed inputs.

If \code{target_output} is omitted, the name of \code{x} is used, or the left-hand
side of an assignment (\code{tt_data_list(settings <- rows)}). An inline
expression such as \code{grid |> group_split(row_number())} is not a name, so
name it with \verb{<-} or pass \code{target_output}.
}
\examples{
\dontrun{
grid <- expand.grid(alpha = c(0, 1), lambda = c(0.1, 1))
pipeline <- tt_initialise(store = "store") |>
  tt_data_list(settings <- grid |> split(seq_len(nrow(grid))))
}
}
